# Copy to .env and fill in. .env is gitignored and must never be committed —
# hack/secret-scan.sh enforces that in the pre-commit hook and in CI.
#
# Names only, values empty. Never put a real key in this file, and never put a
# base URL that carries a token in its query string.

# The endpoints each profile names (base_url_env / api_key_env in
# profiles.yaml), read by FromEnv. Provider-named, so one .env serves every
# application that uses the library. No trailing /chat/completions on a chat
# URL: the client appends it. The OpenAI URL keeps its /v1.
ZAI_BASE_URL=
ZAI_API_KEY=
MIMO_BASE_URL=
MIMO_API_KEY=
OPENAI_BASE_URL=
OPENAI_API_KEY=

# LiteLLM proxy, when one is reachable.
LITELLM_BASE_URL=
LITELLM_API_KEY=

# Evals call real, paid endpoints. Unset means every eval skips, which is what
# CI relies on. Set to 1 only for a deliberate local run, and set it in the REAL
# environment: it is the flag that makes the harness load this file, so a value
# here is never seen. Everything below may live here; a value already in the
# environment wins, and values are bare (no quotes, no inline comments).
LLMWIRE_EVAL=

# The endpoints the probes measure. A missing pair skips that vendor's probes
# with a named reason. No trailing /chat/completions.
LLMWIRE_EVAL_MIMO_BASE_URL=
LLMWIRE_EVAL_MIMO_API_KEY=
LLMWIRE_EVAL_ZAI_BASE_URL=
LLMWIRE_EVAL_ZAI_API_KEY=

# Spend circuit breakers for that run. These may be raised, never disabled.
# Defaults apply when unset: 0.50 USD, 60 calls.
LLMWIRE_EVAL_MAX_USD=
LLMWIRE_EVAL_MAX_CREDITS=
LLMWIRE_EVAL_MAX_CALLS=
